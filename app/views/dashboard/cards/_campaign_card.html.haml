= builder.render options.merge(partial: "dashboard/cards/card") do
  - builder.define Cards::CardExhibit::CARD_PROGRESS_BLOCK, progress: 3
  - builder.append Cards::CardExhibit::CARD_PROGRESS_BLOCK do
    -# TODO: GOAL????
    = builder.render :card_progress_total
    = builder.render :card_progress_goal, label: "Collective Goal"
  -# TODO: get rid of the defaults portion below and just use collection directly once blocks is upgraded
  - builder.define Cards::CardExhibit::CARD_DETAILS_BLOCK do
    -# Removed